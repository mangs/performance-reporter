import { measureCLS, measureLCP } from './measure.mts';
// import { onCLS, onLCP } from 'web-vitals';

console.log('hi');
// eslint-disable-next-line no-console -- testing
measureCLS(console.info);
// measureLCP(console.info);
// onLCP(console.info);
// onCLS(console.log);
